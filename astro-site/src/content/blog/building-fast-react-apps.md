---
postTitle: "Building fast React apps"
postImage: "/images/blog/building-fast-react-apps.jpg"
publishDate: 2023-10-12
metaTitle: "Building fast React apps"
metaDescription: "Notes on keeping React apps fast as they grow."
tags: ["react", "performance"]
---

Replace this with the real post body. Standard Markdown works here — headings, **bold**, _italics_, [links](/), and blockquotes:

> A quote block, replacing the old BlogQuote slice.

Code blocks (replacing the old CodeSnippet slice) work with fenced code and get syntax highlighting via Shiki out of the box:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
```
